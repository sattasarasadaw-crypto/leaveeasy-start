// ─────────────────────────────────────────────────────────────
// js/config.local.example.js — แม่แบบ ให้คัดลอกเป็น js/config.local.js
// แล้วใส่คีย์ OpenRouter ของตัวเองแทน "ใส่คีย์ของคุณตรงนี้"
// (ไฟล์ config.local.js ตัวจริงถูกกันไว้ไม่ให้ขึ้น GitHub)
// ─────────────────────────────────────────────────────────────

export const OPENROUTER_API_KEY = "ใส่คีย์ของคุณตรงนี้";
